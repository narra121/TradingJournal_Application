## Firebase environment variables removed as part of decommission (see docs/migration-firebase-to-aws.md)
## AWS-only configuration:
# VITE_API_BASE_URL can be overridden per environment; default baked into config.ts if unset.