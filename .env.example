# Feature flag for backend selection: 'firebase' or 'aws'
VITE_BACKEND_MODE=firebase
# AWS API base URL (set when switching to aws)
VITE_API_BASE_URL=https://your-api-id.execute-api.region.amazonaws.com/v1
